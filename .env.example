# Example environment variables for local development
ES_URL=http://localhost:9200
KIBANA_URL=http://localhost:5601
# ES_USERNAME=elastic
# ES_PASSWORD=changeme

# To bind-mount Elasticsearch data to a host folder (instead of using the
# default named volume "esdata"), set ESDATA_PATH. Examples:
# ESDATA_PATH=./.esdata
# ESDATA_PATH=/Users/you/data/esdata
#ESDATA_PATH=./.esdata

# Enable ES trade logging by default and pick a stable index name
TRADES_ES_ENABLED=1
TRADES_ES_INDEX=trades
TRADES_ES_SEED_INDEX=trades_seed

# Only these algos print to console (others write to logs/)
CONSOLE_ALGOS=CCI14_200_TradingAlgorithm

# Optionally let main_class.py bootstrap ES/Kibana on startup
# BOOTSTRAP_ES=1
